[package]
name = "pmc-application"
version = "0.2.0-beta"
edition = "2021"
rust-version = "1.88"

[dependencies]
pmc-domain = { path = "../pmc-domain" }
pmc-knowledge = { path = "../pmc-knowledge" }
pmc-ledger = { path = "../pmc-ledger" }
pmc-platform = { path = "../pmc-platform" }
# The sample workspace's seed manifest (item ⑨; product owner 2026-09-23).
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
sha2 = "=0.10.9"

[lib]
path = "src/lib.rs"
