{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "windows": ["main"],
  "permissions": [
    "fs:default",
    "filesystem:default",
    "shell:default",
    "http:default",
    "https:default",
    "network:default",
    "uri:default",
    "sql:default",
    "opener:default",
    "process:default"
  ]
}
